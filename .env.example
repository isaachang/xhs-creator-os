# 可选：配置后默认使用 Apify → SocialDataX。
APIFY_API_TOKEN=

# 可选：不填时使用内置的 SocialDataX Actor。
# APIFY_XHS_ACTOR=socialdatax~socialdatax-xhs-data-api

# 可选：只有 MediaCrawler 不在与 xhs-creator-os 同级目录时才需要填写。
# MEDIA_CRAWLER_ROOT=/absolute/path/to/MediaCrawler
